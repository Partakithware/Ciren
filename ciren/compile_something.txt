You can now run make to build the compiler itself,
then ./ciren main.ci -o out.c && gcc out.c -o main && ./main to compile and run a .ci file. 
The --dump-tokens and --dump-ast flags are there for debugging.